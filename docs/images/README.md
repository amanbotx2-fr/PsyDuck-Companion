# Release screenshots

Captured views from the running v1.0.0 development build:

- `hero.png` — desktop companion idle state
- `chat.png` — AI input bubble
- `preferences.png` — Preferences window
- `reminders.png` — reminder manager
- `planner.png` — Daily Planner
- `sticky-notes.png` — sticky message panel
- `pomodoro.png` — active Pomodoro timer
- `about.png` — About dialog

The AI Model Explorer requires a configured provider and was not captured without exposing credentials. It remains intentionally omitted rather than represented by a synthetic image.

The README links this checklist so release screenshots can be added without changing documentation structure.
